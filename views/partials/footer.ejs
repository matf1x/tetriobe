    <script>
        var isAlert = "<%= isAlert %>"; 
        if (isAlert === true) {
            var alertTitle = "<%= alertTitle %>";

            console.log(alertTitle);
            alert(alertTitle);
        }
    </script>

    <!-- Custom scripts -->
    <script src="/js/main.js"></script>